# Parameter Settings

Use `Parameters` to inspect and edit supported runtime parameters.

## Common Actions

- select an instance,
- filter parameters,
- open editable parameters,
- save approved changes,
- review parameter edit history.

Only parameters marked editable can be changed from the UI.

