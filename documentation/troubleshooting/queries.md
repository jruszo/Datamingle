# Query Problems

## Query Fails

Common causes include:

- unsupported SQL syntax,
- blocked `SELECT *`,
- blocked sensitive system table access,
- insufficient query privileges,
- data masking validation errors,
- maximum execution time exceeded,
- agent command failure.

## Query Returns Fewer Rows Than Expected

Datamingle may reduce row limits through system settings or query privilege checks.

