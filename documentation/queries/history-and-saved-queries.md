# Query History And Saved Queries

The query page includes query logs and saved common queries.

## Query Logs

Use query logs to search previous work by:

- SQL text,
- database,
- user,
- alias,
- date range.

## Saved Queries

Useful query history items can be starred and assigned aliases. Reuse saved SQL carefully and confirm that the selected instance and database are still correct.

